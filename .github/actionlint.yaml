# actionlint configuration.
# Declares custom self-hosted runner labels so `actionlint` does not flag
# them as unknown. These labels are provided by the organization's
# protected runner group (databrickslabs-protected-runner-group).
self-hosted-runner:
  labels:
    - linux-ubuntu-latest
